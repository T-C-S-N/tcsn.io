import Memo from "@/components/Memo"
import { CopyBlock, CodeBlock } from "react-code-blocks";

export default function TutoNextReact() {
   return (
      <Memo>
         <div>
            <h2>NextJs, Typescript</h2>
            {/*
            <p>
               Let's start with the basics. NextJs is a framework for ReactJs. It
               allows you to create a ReactJs application with a server side
               rendering. It also allows you to create a static website.
            </p>

            <CodeBlock language={'javascript'} showLineNumbers={true} theme={'material-dark'} wrapLines={true} text={`
             // code
             function add(a, b) {
               return a + b;
               }
             `} />
             */}
         </div>
      </Memo>
   );
}