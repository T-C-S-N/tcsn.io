# Cloudflare Pages redirects
# Redirect API calls to the Cloudflare Worker

/api/* https://tcsn-io-api.tomas-bbe.workers.dev/api/:splat 200

# SPA fallback - serve index.html for all non-API routes
/* /index.html 200
